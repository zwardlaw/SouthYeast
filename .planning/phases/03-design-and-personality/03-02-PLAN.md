---
phase: 03-design-and-personality
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - TakeMeToPizza/Views/CarouselView.swift
  - TakeMeToPizza/ContentView.swift
  - TakeMeToPizza/Views/CompassView.swift
  - TakeMeToPizza/Views/OnboardingView.swift
  - TakeMeToPizza/Views/MysteryModeModifier.swift
  - TakeMeToPizza.xcodeproj/project.pbxproj
autonomous: false

must_haves:
  truths:
    - "Every screen uses the neobrutalist design system -- thick borders, hard shadows, bold typography, and pizza color palette"
    - "Mystery mode hides all restaurant names throughout the app when activated via the leftmost card"
    - "First-time users see an onboarding screen that explains the compass concept before the main UI"
    - "Location permission is preceded by a priming screen with playful pizza personality"
    - "Mystery mode state persists across app launches"
  artifacts:
    - path: "TakeMeToPizza/Views/OnboardingView.swift"
      provides: "Single-screen onboarding with pizza slice illustration"
      contains: "struct OnboardingView"
    - path: "TakeMeToPizza/Views/MysteryModeModifier.swift"
      provides: "Redaction overlay + MysteryToggleCard"
      contains: "struct MysteryRedacted"
    - path: "TakeMeToPizza/Views/CarouselView.swift"
      provides: "Brutalist-styled cards with mystery mode redaction"
      contains: "mysteryRedacted"
    - path: "TakeMeToPizza/ContentView.swift"
      provides: "Onboarding gate before permission flow"
      contains: "hasCompletedOnboarding"
  key_links:
    - from: "TakeMeToPizza/Views/CarouselView.swift"
      to: "TakeMeToPizza/Views/MysteryModeModifier.swift"
      via: "mysteryRedacted modifier on place name and address"
      pattern: "\\.mysteryRedacted"
    - from: "TakeMeToPizza/Views/CarouselView.swift"
      to: "@AppStorage mystery mode key"
      via: "@AppStorage(AppStorageKey.mysteryMode)"
      pattern: "AppStorageKey\\.mysteryMode"
    - from: "TakeMeToPizza/ContentView.swift"
      to: "TakeMeToPizza/Views/OnboardingView.swift"
      via: "onboarding gate wrapping permission switch"
      pattern: "hasCompletedOnboarding"
    - from: "TakeMeToPizza/Views/OnboardingView.swift"
      to: "TakeMeToPizza/Views/PizzaSliceNeedle.swift"
      via: "PizzaSliceShape used as illustration"
      pattern: "PizzaSliceShape"
---

<objective>
Apply the neobrutalist design system to every screen (carousel cards, permission views, error states), add mystery mode as a hidden easter egg in the carousel, and create the onboarding + location priming flow. This plan transforms every user-facing surface to match the Take Me to Pizza visual identity.

Purpose: Plan 01 built the design token infrastructure and hero needle. This plan applies those tokens everywhere else and adds the personality features (mystery mode, onboarding) that make the app surprising and memorable.

Output: Every screen styled with brutalist design tokens. Mystery mode toggle as leftmost carousel card with classified-document redaction. Single-screen onboarding for first-time users. Playful location priming screen.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-and-personality/03-CONTEXT.md
@.planning/phases/03-design-and-personality/03-RESEARCH.md
@.planning/phases/03-design-and-personality/03-01-SUMMARY.md

@TakeMeToPizza/Views/CarouselView.swift
@TakeMeToPizza/Views/CompassView.swift
@TakeMeToPizza/ContentView.swift
@TakeMeToPizza/DesignSystem/Colors.swift
@TakeMeToPizza/DesignSystem/Typography.swift
@TakeMeToPizza/DesignSystem/BrutalistModifiers.swift
@TakeMeToPizza/DesignSystem/AppStorageKey.swift
@TakeMeToPizza/Views/PizzaSliceNeedle.swift
@TakeMeToPizza.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle all views with neobrutalist design system</name>
  <files>
    TakeMeToPizza/Views/CarouselView.swift
    TakeMeToPizza/Views/CompassView.swift
    TakeMeToPizza/ContentView.swift
  </files>
  <action>
    Apply the design system tokens from Plan 01 to every existing view. This is a restyle pass -- behavior stays identical, only visual appearance changes.

    **CarouselView.swift -- CardView restyle:**
    - Replace `.background(.regularMaterial, in: RoundedRectangle(cornerRadius: 16))` with `.brutalistCard()` on the card's outer VStack
    - Replace `.font(.headline)` on place name with `.font(.pizzaBody(size: 16))` and add `.fontWeight(.semibold)`
    - Replace `.font(.subheadline)` on distance with `.font(.pizzaBody(size: 14))`
    - Replace `.font(.caption)` on address/phone/website with `.font(.pizzaBody(size: 12))`
    - Replace the directions button styling: remove the manual `.foregroundStyle(.white) .background(Color.orange) .clipShape(...)` chain and use `.brutalistButton()` on the button label. Keep the HStack with arrow icon + "Get Directions" text inside, but set font to `.pizzaDisplay(size: 16)`.
    - Replace `.tint(.orange)` on the website Link with `.tint(.pizzaOrange)`
    - Update `@AppStorage("preferredMapsApp")` to `@AppStorage(AppStorageKey.preferredMapsApp)` and `@AppStorage("hasChosenMapsApp")` to `@AppStorage(AppStorageKey.hasChosenMapsApp)` -- centralized key constants

    **CarouselView.swift -- Carousel container restyle:**
    - Replace `.background(.ultraThinMaterial, in: RoundedRectangle(cornerRadius: 20))` with `.background(Color.pizzaCard.opacity(0.95), in: RoundedRectangle(cornerRadius: 16))` and add `.overlay(RoundedRectangle(cornerRadius: 16).stroke(Color.primary, lineWidth: 2))` -- brutalist container instead of material blur

    **CarouselView.swift -- StateCard restyle:**
    - Replace `.font(.system(size: 32))` with `.font(.system(size: 36))` and `.foregroundStyle(.pizzaOrange)` (instead of `.secondary`)
    - Replace `.font(.headline)` with `.font(.pizzaDisplay(size: 20))`
    - Replace `.font(.caption)` with `.font(.pizzaBody(size: 13))`

    **CarouselView.swift -- LoadingSkeleton restyle:**
    - Replace `.fill(Color.secondary.opacity(0.3))` with `.fill(Color.pizzaCard)` and add `.overlay(RoundedRectangle(cornerRadius: 12).stroke(Color.primary.opacity(0.3), lineWidth: 1.5))`
    - Remove `.redacted(reason: .placeholder)` -- the brutalist skeleton is the styled empty card itself

    **CompassView.swift adjustments (if not already done in Plan 01):**
    - Verify background is `Color.pizzaBackground.ignoresSafeArea()`
    - Verify target place name uses `.font(.pizzaDisplay(size: 24))`
    - Calibration state: use `.font(.pizzaDisplay(size: 20))` for "Calibrating..." and `.font(.pizzaBody(size: 14))` for the subtitle

    **ContentView.swift -- Permission views restyle:**

    `PermissionPrimingView`:
    - Replace `Image(systemName: "location.north.fill") .font(.system(size: 80)) .foregroundStyle(.orange)` with a `PizzaSliceShape().fill(Color.pizzaOrange).frame(width: 100, height: 100)` -- the pizza slice IS the location icon. Add a small rotation animation (subtle wiggle) to make it feel alive.
    - Replace "Find Nearby Pizza" with "FOLLOW THE PIZZA" using `.font(.pizzaDisplay(size: 32))`
    - Replace the body text with something playful: "This little pizza slice is lost without you. Share your location so it can point you to the nearest pizza." using `.font(.pizzaBody(size: 16))`
    - Replace the button styling with `.brutalistButton()`. Change button text to "LET'S FIND PIZZA" using `.font(.pizzaDisplay(size: 18))`
    - Add `Color.pizzaBackground.ignoresSafeArea()` as the view background

    `PermissionDeniedView`:
    - Replace SF Symbol with `PizzaSliceShape().fill(Color.secondary.opacity(0.4)).frame(width: 80, height: 80).rotationEffect(.degrees(180))` -- upside-down sad pizza slice
    - Replace "Location Denied" with "PIZZA SLICE IS SAD" using `.font(.pizzaDisplay(size: 28))`
    - Update body text: "The pizza slice can't find pizza without your location. You can fix this in Settings." using `.font(.pizzaBody(size: 16))`
    - Button: "OPEN SETTINGS" with `.brutalistButton()`
    - Background: `Color.pizzaBackground.ignoresSafeArea()`

    `PermissionRestrictedView`:
    - Same treatment as DeniedView but keep the lock icon (SF Symbol) as this is a system restriction. Style it with `.foregroundStyle(.pizzaOrange)`
    - "LOCATION RESTRICTED" in `.pizzaDisplay(size: 28)`
    - Body text in `.pizzaBody(size: 16)`
    - Background: `Color.pizzaBackground.ignoresSafeArea()`

    IMPORTANT: Do NOT change any behavioral logic. Carousel scrolling, card expansion, directions handoff, permission requesting -- all stay identical. Only fonts, colors, backgrounds, and borders change.
  </action>
  <verify>
    `xcodebuild build -project TakeMeToPizza.xcodeproj -scheme TakeMeToPizza -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5` succeeds.
    `grep -c "pizzaDisplay\|pizzaBody\|brutalistCard\|brutalistButton\|pizzaBackground\|pizzaOrange\|pizzaRed\|pizzaGold\|pizzaCard" TakeMeToPizza/Views/CarouselView.swift` returns >= 5 (design tokens in use).
    `grep -c "Color.orange" TakeMeToPizza/Views/CarouselView.swift` returns 0 (no raw orange left).
    `grep -c "pizzaDisplay\|pizzaBody\|brutalistButton\|pizzaBackground" TakeMeToPizza/ContentView.swift` returns >= 4 (permission views styled).
  </verify>
  <done>
    Every user-facing view uses pizza palette colors, custom fonts, and brutalist borders/shadows. No raw system colors (Color.orange) remain in any view file. Permission priming screen has playful pizza personality copy and pizza slice illustration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mystery mode, onboarding flow, and pbxproj registration</name>
  <files>
    TakeMeToPizza/Views/MysteryModeModifier.swift
    TakeMeToPizza/Views/OnboardingView.swift
    TakeMeToPizza/Views/CarouselView.swift
    TakeMeToPizza/ContentView.swift
    TakeMeToPizza.xcodeproj/project.pbxproj
  </files>
  <action>
    **MysteryModeModifier.swift** -- Create in Views/:

    `MysteryRedacted: ViewModifier`:
    - Takes `isActive: Bool` parameter
    - When active: overlays a `RoundedRectangle(cornerRadius: 3).fill(Color.primary).padding(.vertical, 2)` over the content -- this is the "classified document" redaction bar
    - Animate the overlay with `.animation(.easeInOut(duration: 0.2), value: isActive)`
    - Do NOT use SwiftUI's built-in `.redacted(reason: .placeholder)` -- that gives gray shimmer, not black bars
    - View extension: `.mysteryRedacted(isActive:)`

    `MysteryToggleCard: View`:
    - `@AppStorage(AppStorageKey.mysteryMode) private var mysteryModeEnabled: Bool = false`
    - Layout: VStack with centered emoji "..." and conditional "Mystery ON" label
    - The emoji is just a large Text("?") or similar playful character (the actual emoji rendering of the peeking face from the requirements). Use `Text("\u{1FAE3}")` for the 🫣 emoji, or just the literal emoji character in the string.
    - When mystery mode is ON: show the emoji + "MYSTERY ON" text in `.pizzaBody(size: 12)`. When OFF: show just the emoji (no label -- discovery through curiosity).
    - Apply `.brutalistCard()` for the card styling
    - `onTapGesture`: toggle `mysteryModeEnabled` with `withAnimation(.spring(response: 0.3, dampingFraction: 0.7))`
    - Add `.sensoryFeedback(.selection, trigger: mysteryModeEnabled)` for tactile toggle feedback
    - Size: same `cardWidth` as place cards (passed as parameter or via GeometryReader)

    **OnboardingView.swift** -- Create in Views/:
    - Takes `onComplete: () -> Void` closure parameter
    - Layout: ZStack with `Color.pizzaBackground.ignoresSafeArea()`, VStack with:
      - Spacer
      - Large `PizzaSliceShape().fill(Color.pizzaOrange).frame(width: 160, height: 160)` as the illustration
      - "FOLLOW THE PIZZA" in `.pizzaDisplay(size: 36)` centered
      - A brief subtitle: "Your compass to the nearest slice" in `.pizzaBody(size: 16)` with `.foregroundStyle(.secondary)`
      - Spacer
      - Button("LET'S GO") calling `onComplete()`, styled with `.brutalistButton()`, with `.padding(.horizontal, 32)` and `.padding(.bottom, 48)`
    - Add a subtle entrance animation: the pizza slice scales from 0.5 to 1.0 with a spring on appear
    - Minimal and direct -- almost wordless. The pizza slice carries the message.
    - Do NOT mention mystery mode anywhere in onboarding -- pure easter egg.

    **CarouselView.swift -- Wire mystery mode:**
    - Add `@AppStorage(AppStorageKey.mysteryMode) private var mysteryModeEnabled: Bool = false` to `CarouselView`
    - In `placesCarousel`, prepend `MysteryToggleCard` before the `ForEach(placesService.places)` loop inside the `LazyHStack`. Give it the same `cardWidth` frame and an `.id` that won't conflict with place UUIDs (use a fixed UUID via `UUID(uuidString: "00000000-0000-0000-0000-000000000000")!` or a separate namespace).
    - IMPORTANT: The mystery toggle card is the LEFTMOST card (index 0). Place cards start after it. When the user swipes right past the first place card, they discover the mystery toggle organically.
    - In the `onChange(of: scrollID)` handler: add a guard to skip processing if `scrollID` matches the mystery card's fixed ID (it's not a place, so don't try to find it in placesService.places).
    - In `CardView`, apply `.mysteryRedacted(isActive: mysteryModeEnabled)` to:
      - `Text(place.name)` -- the place name
      - `Label(place.address, ...)` -- the address (in expanded state)
      - `Label(phone, ...)` -- the phone number (in expanded state)
      - `Link(destination: website)` -- the website link (in expanded state)
    - Do NOT redact distance (Text(place.distanceDisplayString)) -- per CONTEXT.md "everything except distance is redacted"
    - Do NOT redact the directions button -- you can still get directions in mystery mode
    - Pass `mysteryModeEnabled` to CardView (add it as a property or use @AppStorage in CardView directly with the same key)

    **ContentView.swift -- Wire onboarding gate:**
    - Add `@AppStorage(AppStorageKey.hasCompletedOnboarding) private var hasCompletedOnboarding: Bool = false`
    - Wrap the existing `Group { switch locationService.permissionStatus { ... } }` block:
      ```swift
      Group {
          if !hasCompletedOnboarding {
              OnboardingView(onComplete: { hasCompletedOnboarding = true })
          } else {
              // existing permission switch
              switch locationService.permissionStatus {
              ...
              }
          }
      }
      ```
    - The `.onChange(of: scenePhase)` and other modifiers stay on the outer Group so they always fire regardless of onboarding state.

    **project.pbxproj -- Register 2 new files:**
    - Read existing pbxproj first to find current ID state
    - IDs from Plan 01 go up to approximately AA000025/AA000127. Use next available IDs.
    - PBXFileReference: OnboardingView.swift, MysteryModeModifier.swift (in Views group)
    - PBXBuildFile: both in Sources build phase
    - Add both file references to the Views PBXGroup children array

    After all changes, run build to verify.
  </action>
  <verify>
    `xcodebuild build -project TakeMeToPizza.xcodeproj -scheme TakeMeToPizza -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5` succeeds.
    `grep -c "mysteryRedacted" TakeMeToPizza/Views/CarouselView.swift` returns >= 2 (name + at least one expanded field redacted).
    `grep -c "MysteryToggleCard" TakeMeToPizza/Views/CarouselView.swift` returns >= 1 (toggle card in carousel).
    `grep -c "hasCompletedOnboarding" TakeMeToPizza/ContentView.swift` returns >= 2 (declaration + condition check).
    `grep -c "OnboardingView" TakeMeToPizza/ContentView.swift` returns >= 1 (onboarding view shown).
    `grep -c "OnboardingView\|MysteryModeModifier" TakeMeToPizza.xcodeproj/project.pbxproj` returns >= 2 (registered in pbxproj).
  </verify>
  <done>
    Mystery mode: MysteryToggleCard appears as leftmost carousel card, tapping toggles @AppStorage-persisted mode that redacts place names/addresses/phone/website with classified-document bars. Onboarding: first-time users see a single screen with pizza slice illustration and "FOLLOW THE PIZZA" before entering the permission flow, never shown again after completion. Both registered in pbxproj, build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete neobrutalist design system applied to every screen in the app. Mystery mode easter egg accessible by swiping right past the first carousel card. Onboarding screen for first-time users. Pizza slice compass needle with tilt parallax (requires physical device to test tilt).

    This checkpoint covers ALL of Phase 3 -- the full visual identity and personality of Take Me to Pizza.
  </what-built>
  <how-to-verify>
    Build and run on a physical iOS device (or Simulator for non-motion features):

    1. **Onboarding (first launch):** Delete app if installed, then launch fresh. Verify single onboarding screen appears with pizza slice illustration and "FOLLOW THE PIZZA" heading. Tap "LET'S GO" to proceed. Kill and relaunch -- onboarding should NOT appear again.

    2. **Location priming:** After onboarding, verify the location priming screen has playful pizza personality (pizza slice illustration, warm copy, brutalist-styled button). Grant location permission.

    3. **Compass needle:** Verify the compass shows a pizza slice shape (not an SF Symbol arrow). On physical device: tilt the phone and verify the pizza slice has a subtle 3D parallax tilt effect. Rotate the phone and verify the slice rotates to track the nearest pizza place with smooth spring animation.

    4. **Alignment feedback:** Point the phone toward the targeted pizza place. Verify a warm glow appears around the needle when aligned.

    5. **Design system on carousel:** Scroll the carousel. Verify cards have thick borders, hard offset shadows, bold typography (Bebas Neue headers, Space Grotesk body), and pizza warm colors. Tap a card to expand -- verify details use design tokens. Verify directions button has brutalist styling.

    6. **Mystery mode:** Swipe RIGHT past the first place card in the carousel to reveal the mystery toggle card (shows an emoji). Tap it. Verify:
       - Place names get blacked-out redaction bars (solid, not gray shimmer)
       - Distance remains visible
       - Address, phone, website in expanded cards are also redacted
       - Directions button still works
       - Kill and relaunch -- mystery mode should still be ON

    7. **Dark mode:** Toggle iOS dark mode in Settings. Verify all colors adapt: warm dark background (not cold gray), card backgrounds shift to warm brown-black, borders switch to white, pizza palette adjusts.

    8. **Permission denied view:** (Optional) Revoke location in Settings, reopen app. Verify denied view has brutalist styling and pizza personality.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 3, or describe any visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `xcodebuild build` succeeds with zero errors
2. Every view file uses design tokens (no raw Color.orange, no system .headline/.body fonts without pizza* wrapper)
3. OnboardingView.swift exists and is gated in ContentView via @AppStorage
4. MysteryModeModifier.swift exists with solid redaction overlay (not .redacted(reason:))
5. MysteryToggleCard prepended as leftmost card in carousel LazyHStack
6. @AppStorage keys use AppStorageKey constants (no raw strings for mystery mode or onboarding)
7. Dark mode: all Color references use either .pizzaX tokens (Asset Catalog adaptive) or .primary (auto-adapts)
</verification>

<success_criteria>
- PERS-01: Mystery mode activated via leftmost carousel card with emoji toggle
- PERS-02: Mystery mode hides restaurant names/address/phone/website, shows only compass + distance
- PERS-03: Onboarding flow shows pizza slice illustration + "FOLLOW THE PIZZA" on first launch only
- PERS-04: Neobrutalist design system applied to every screen (carousel, compass, permission views, error states)
- PERS-05: Location permission priming screen has playful pizza personality copy and illustration
- All views render correctly in both light and dark mode
- Mystery mode persists across app launches via @AppStorage/UserDefaults
- Human-verify checkpoint approved on physical device
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-and-personality/03-02-SUMMARY.md`
</output>

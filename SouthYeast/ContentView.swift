import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "location.north.fill")
                .font(.system(size: 80))
            Text("SouthYeast")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}
